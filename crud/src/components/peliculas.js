/*import React, {Component} from 'react';
import ListItem from '@material-ui/core/ListItem';
import Divider from '@material-ui/core/Divider';
import ListItemText from '@material-ui/core/ListItemText';
import ListItemAvatar from '@material-ui/core/ListItemAvatar';
import Avatar from '@material-ui/core/Avatar';
import Typography from '@material-ui/core/Typography';

/*class Peliculas extends Component{
    render(){
        return this.props.data.map(e => <p  id="hello"> <h1>{e.titulo}</h1> {e.description} <br />
            <b>Director:</b> {e.director} <br />
            <b>Producción:</b> {e.produccion} <br />
            <b>Duración:</b> {e.duracion} <div id="imagen"></div></p>)
    }
}

class Movies extends Component{
    render(){
        return this.props.data.map(e => 
        <div><ListItem alignItems="flex-start">
        <ListItemAvatar>
            <Avatar alt="Imagen" src={require(e.imagen)} />
        </ListItemAvatar>
        <ListItemText
          primary={e.titulo}
          secondary={
            <React.Fragment>
              <Typography
                component="span"
                variant="body2"
                className={e.description}
                color="textPrimary"
              >
                <b>Director:</b> {e.director} <br /> <b>Producción:</b> {e.produccion} <br />
              </Typography>
              <b>Duración:</b> {e.duracion}
            </React.Fragment>
          }
        />
        </ListItem>
        <Divider variant="inset" component="li" />
        </div>)
    }
}

export default Movies;*/